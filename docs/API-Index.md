# API Index — FinderX 📚

Purpose

A central entry point for API documentation and plugin development resources.

Quick links

- Plugin API: `docs/plugin-api.md`
- Themes: `docs/themes.md`
- Example plugin: `examples/recent-files-plugin`

Notable classes (short list)

- `dev.eministar.FinderXApp` — main application entry
- Index/search components — (TODO: list concrete classes / generate via Javadoc)

Future

Consider adding automated Javadoc generation in CI and linking the generated HTML from here.
